{
  "name": "kaff-gui-api",
  "version": "0.1.0",
  "private": true,
  "description": "Tooling JS minimal (commitlint + husky) — la stack runtime est en Python (voir pyproject.toml)",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.3",
    "@commitlint/config-conventional": "^20.5.3",
    "husky": "^9.1.7"
  }
}
